<% content_for :title, "Dashboard | DiabetCare" %>

<main class="dashboard-page">
  <section class="dashboard-hero">
    <div>
      <p class="eyebrow">Dashboard patient</p>
      <h1>Bonjour <%= @patient_name %>, voici votre journee.</h1>
      <p class="hero-copy">
        Retrouvez vos indicateurs clefs, vos rappels importants et les prochains
        points de suivi au meme endroit.
      </p>
    </div>

    <div class="hero-panel">
      <span class="hero-label">Statut du jour</span>
      <strong>Suivi global stable</strong>
      <p>Derniere synchronisation il y a 5 minutes.</p>
    </div>
  </section>

  <section class="summary-grid">
    <% @summary_cards.each do |card| %>
      <article class="summary-card">
        <p class="summary-label"><%= card[:label] %></p>
        <p class="summary-value"><%= card[:value] %></p>
        <p class="summary-trend summary-trend--<%= card[:tone] %>"><%= card[:trend] %></p>
      </article>
    <% end %>
  </section>

  <section class="dashboard-grid">
    <article class="panel">
      <div class="panel-header">
        <h2>Planning du jour</h2>
        <span>Aujourd'hui</span>
      </div>

      <div class="timeline">
        <% @today_timeline.each do |item| %>
          <div class="timeline-item">
            <p class="timeline-time"><%= item[:time] %></p>
            <div>
              <h3><%= item[:title] %></h3>
              <p><%= item[:detail] %></p>
            </div>
          </div>
        <% end %>
      </div>
    </article>

    <article class="panel">
      <div class="panel-header">
        <h2>Points d'attention</h2>
        <span>Analyse rapide</span>
      </div>

      <ul class="insight-list">
        <% @insights.each do |insight| %>
          <li><%= insight %></li>
        <% end %>
      </ul>

      <div class="next-step">
        <p class="next-step-label">Prochaine evolution utile</p>
        <p>Connecter ces widgets a de vraies donnees de glycemie, repas et traitements.</p>
      </div>
    </article>
  </section>
</main>
